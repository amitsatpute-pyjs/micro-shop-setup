apiVersion: apps/v1
kind: Deployment
metadata:
  name: prodcuts-deployment
  labels:
    name: prodcuts-deployment
    app: micro-shop
spec:
  selector:
    matchLabels:
      name: prodcuts-pod
      app: micro-shop
  replicas: 1
  template:
    metadata:
      name: prodcuts-pod
      labels:
        name: prodcuts-pod
        app: micro-shop
    spec:
      restartPolicy: Always
      containers:
      - name: prodcuts
        image: products:latest
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 8082
        imagePullPolicy: IfNotPresent
        env:
        - name: DATABASE_USER
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: db-root-username
        - name: DATABASE_PASSWORD
          valueFrom:
            secretKeyRef:
              name: mysql-secret
              key: db-root-password
        - name: DATABASE_HOST
          valueFrom:
            configMapKeyRef:
              name: mysql-configmap
              key: database_url
        - name: DATABASE_PORT
          valueFrom:
            configMapKeyRef:
              name: mysql-configmap
              key: database_port
        - name: DATABASE_NAME
          valueFrom:
            configMapKeyRef:
              name: mysql-configmap
              key: products_database_name


